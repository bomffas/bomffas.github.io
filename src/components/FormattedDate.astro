---
interface PFormattedDate {
  date: Date;
}

const { date } = Astro.props as PFormattedDate;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("pt-br", {
      year: "numeric",
      month: "numeric",
      day: "numeric",
    })
  }
</time>
